import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'avis-sondage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
